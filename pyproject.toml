[project]
name = "lte-bot"
version = "0.1.0"
description = "Discord bot that monitors LTE data usage from pass.telekom.de"
requires-python = ">=3.11"
dependencies = [
    "discord-py>=2.5",
    "python-dotenv>=1.0",
    "aiohttp>=3.9",
]
